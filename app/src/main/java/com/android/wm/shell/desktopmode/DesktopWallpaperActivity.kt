package com.android.wm.shell.desktopmode

import android.app.Activity

/** Inert manifest-compatible endpoint supplied by the standalone launcher build. */
class DesktopWallpaperActivity : Activity()
