package com.android.car.carlaunchercommon

/** Shared constants intentionally free of feature dependencies for Dock and AppGrid consumers. */
object LauncherCommonContract {
    const val STOCK_APP_ORDER_FILE = "order.data"
    const val DOCK_PROTO_FILE = "dock_items.pb"
}
