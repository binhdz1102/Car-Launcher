package com.android.car.carlauncher.feature.launcher.domain

import com.android.car.carlauncher.feature.home.domain.HomeTaskStateMachine

/** @deprecated HOME owns this state machine. Kept for source-compatible feature migration. */
typealias LauncherPaneStateMachine = HomeTaskStateMachine
